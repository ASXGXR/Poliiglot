name: Inject API Key

on: [push]

jobs:
  inject:
    runs-on: ubuntu-latest

    steps:
    - name: Inject API key into environment
      run: echo "API_KEY=${{ secrets.OPENAI_KEY }}" >> $GITHUB_ENV
